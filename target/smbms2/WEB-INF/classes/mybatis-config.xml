<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!--    从db.properties读取数据库属性-->
    <properties resource="db.properties"/>
<!--    通过包名设定类别名-->
    <settings>
        <setting name="logImpl" value="LOG4J"/>
    </settings>
    <typeAliases>
        <package name="Bean"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${DRIVER}"/>
                <property name="url" value="${URL}"/>
                <property name="username" value="${USERNAME}"/>
                <property name="password" value=""/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
<!--        book mapper in package dao.book-->
<!--        方式2： 通过class注册mapper， mapper的文件名必须和类名保持一致, 且必须在同一个包下！！！-->
        <mapper class="dao.book.BookMapper"></mapper>
        <!--        方式1： 通过resource注册mapper-->
        <!--        <mapper resource="dao/book/BookMapper.xml"></mapper>-->
    </mappers>
</configuration>